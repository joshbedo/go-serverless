service: serverless-go
frameworkVersion: '3'
provider: 
  name: aws
  runtime: go1.x
  region: us-east-2
  stage: ${opt:stage, 'dev'}
  environment:
    DSN: ysc4ja7ymki8ge7isa0x:pscale_pw_PL8axrBhVKVqt0wD3g29XOgObqO97OxmAPomP19vTk4@tcp(aws.connect.psdb.cloud)/testing?tls=true&interpolateParams=true
    stage: ${opt:stage, 'dev'}

functions:
  api:
    handler: bin/main
    events:
      - http:
          path: /{proxy+}  # Use a wildcard for the path
          method: ANY
          cors: true

package:
  patterns:
    - bin/**
    - go.mod
    - go.sum